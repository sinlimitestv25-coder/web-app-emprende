import { platformEvents } from "../../data/demo";

export function Activity() {
  return <><div className="page-heading"><div><p className="eyebrow">Trazabilidad</p><h1>Actividad administrativa</h1><p>Registro de accesos y cambios de plataforma, sin contenido comercial.</p></div><button className="button secondary">Descargar auditoría</button></div><div className="audit-filters"><button className="active">Todos</button><button>Accesos</button><button>Espacios</button><button>Usuarios</button><button>Configuración</button></div><section className="panel audit-panel"><div className="audit-head"><span>Evento</span><span>Realizado por</span><span>Contexto</span><span>Fecha</span></div>{platformEvents.map((event) => <div className="audit-row" key={event.id}><div><span className={`activity-icon ${event.tone}`}>{event.action.charAt(0)}</span><strong>{event.action}</strong></div><span>{event.subject}</span><span>{event.context}</span><span>{event.time}</span></div>)}</section><div className="audit-privacy"><span>✓</span><p><strong>Registro minimizado</strong>La auditoría guarda quién hizo una acción administrativa y cuándo. No almacena nombres de clientes, pedidos, productos ni importes de ventas.</p></div></>;
}
