import { products } from "../../data/demo";

export function Inventory() {
  return <><div className="page-heading"><div><p className="eyebrow">Luna Creativa · NX-0001</p><h1>Inventario</h1><p>Stock físico, reservado y disponible por variante.</p></div><div className="heading-actions"><button className="button secondary">Registrar movimiento</button><button className="button primary">+ Nuevo producto</button></div></div><div className="inventory-alert"><span>!</span><div><strong>2 productos necesitan atención</strong><p>Un producto tiene stock bajo y otro se ocultó del portal por falta de unidades.</p></div><button>Revisar alertas</button></div><section className="panel table-panel"><div className="table-tools"><div className="search-field">⌕<input aria-label="Buscar productos" placeholder="Buscar producto o variante" /></div><button className="select-button">Todas las categorías⌄</button></div><div className="inventory-table"><div className="inventory-head"><span>Producto / variante</span><span>Físico</span><span>Reservado</span><span>Disponible</span><span>Precio</span><span>Estado</span></div>{products.map((product) => <div className="inventory-row" key={product.name}><div><span className="product-thumb">{product.name.charAt(0)}</span><div><strong>{product.name}</strong><small>{product.variant}</small></div></div><b>{product.stock}</b><span>{product.reserved}</span><strong>{Math.max(product.stock-product.reserved,0)}</strong><span>{product.price}</span><span className={`status ${product.state === "Disponible" ? "success" : product.state === "Bajo" ? "status-nuevo" : "danger"}`}>{product.state}</span></div>)}</div></section></>;
}
