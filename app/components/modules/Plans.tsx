import { modules, plans } from "../../data/demo";

export function Plans() {
  return <><div className="page-heading"><div><p className="eyebrow">Configuración comercial</p><h1>Planes y módulos</h1><p>Definí qué puede utilizar cada emprendimiento y cuántos usuarios incluye.</p></div><button className="button primary">+ Nuevo plan</button></div><div className="plans-grid">{plans.map((plan) => <article className={`plan-card plan-${plan.accent}`} key={plan.name}><div className="plan-card-top"><span>{plan.name}</span><button>•••</button></div><strong>{plan.price}</strong><small>{plan.name === "Prueba" ? "sin cargo" : "por mes"}</small><dl><div><dt>Usuarios</dt><dd>{plan.users}</dd></div><div><dt>Almacenamiento</dt><dd>{plan.storage}</dd></div><div><dt>Módulos incluidos</dt><dd>{plan.modules}</dd></div></dl><button className="button secondary full">Editar plan</button></article>)}</div><section className="panel module-matrix"><div className="panel-title"><div><h2>Catálogo de módulos</h2><p>Disponibles para habilitar por espacio o plan</p></div><span className="status success">{modules.length} disponibles</span></div><div className="module-grid">{modules.map((name, index) => <article key={name}><span>{name.charAt(0)}</span><div><strong>{name}</strong><small>{index < 5 ? "Módulo principal" : "Extensión opcional"}</small></div><button aria-label={`Configurar ${name}`}>→</button></article>)}</div></section><div className="plan-rule"><span>i</span><div><strong>Los cambios no se aplican sin confirmación</strong><p>Modificar un plan no quitará automáticamente funciones a los espacios existentes. Podrás decidir qué clientes migrar.</p></div></div></>;
}
