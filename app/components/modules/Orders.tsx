import { orders } from "../../data/demo";

export function Orders() {
  return <><div className="page-heading"><div><p className="eyebrow">Luna Creativa · NX-0001</p><h1>Pedidos</h1><p>Del carrito a la entrega, con el stock siempre sincronizado.</p></div><button className="button primary">+ Cargar pedido</button></div><div className="order-pipeline">{[{n:"3",l:"Nuevos"},{n:"5",l:"Confirmados"},{n:"4",l:"Preparando"},{n:"3",l:"Listos"},{n:"60",l:"Entregados"}].map((stage,index)=><div key={stage.l} className={index===0?"stage active":"stage"}><strong>{stage.n}</strong><span>{stage.l}</span></div>)}</div><section className="panel table-panel"><div className="panel-title order-title"><div><h2>Pedidos recientes</h2><p>75 pedidos durante agosto</p></div><button className="select-button">Agosto 2026⌄</button></div><div className="orders-table"><div className="orders-head"><span>Pedido</span><span>Cliente</span><span>Detalle</span><span>Estado</span><span>Total</span></div>{orders.map(order=><div className="orders-row" key={order.id}><strong>{order.id}</strong><div><span>{order.customer}</span><small>WhatsApp disponible</small></div><span>{order.item}</span><span className={`status status-${order.status.toLowerCase()}`}>{order.status}</span><b>{order.total}</b></div>)}</div></section></>;
}
