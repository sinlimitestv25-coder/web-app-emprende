# Entrega Nexo v0.8.2

## Qué incluye

- Imagen de fondo propia en el panel izquierdo del login (la que mandaste — comprimida de 1,53 MB a ~96 KB).
- Velo oscuro con degradé sobre la imagen (más oscuro arriba y abajo, un poco más claro en el medio) para que el logo, el eslogan y el título sigan siendo bien legibles sobre una imagen que es bastante clara de por sí.

## Archivos para subir (3 en total, entran en una sola subida)

**Código:**
- `app/globals.css`
- `package.json`
- `README.md`

**Imagen nueva:**
- `public/login-bg.jpg`

## Commit sugerido

```
Nexo v0.8.2: imagen de fondo en el panel izquierdo del login
```

## Orden

Una sola subida, un solo commit.

Esta versión no requiere ejecutar SQL ni subir ningún `dist/` compilado.
